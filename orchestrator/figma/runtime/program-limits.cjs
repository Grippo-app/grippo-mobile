'use strict';

// One owner for limits shared by capture, publication, adapters, API and tests.
// Domain modules may expose narrower named views, but must not redefine values.
module.exports = Object.freeze({
  manifestArtifactsMax: 200,
  compositePublicationArtifactsMax: 195,
  phaseBytesMax: 64 * 1024 * 1024,
  artifactBytesMax: 8 * 1024 * 1024,
  stageManifestBytesMax: 256 * 1024,
  tokenSourceBuckets: 128,
  tokenHealthBuckets: 128,
  tokenSourceRecordsMax: 4096,
  tokenObservationsPerSourceMax: 2048,
  tokenObservationsTotalMax: 250000,
  observedTokenKeysMax: 20000,
  providerNameBytesMax: 512,
  tokenStringValueBytesMax: 16384,
  tokenLogicalCaptureBytesMax: 4 * 1024 * 1024,
  tokenShardsBytesMax: 32 * 1024 * 1024,
  tokenIndexCatalogBytesMax: 16 * 1024 * 1024,
  figmaParallelReadsMax: 4,
  sourceHealthSnapshotsMax: 10,
  jobSummariesMax: 100,
  tokenSourceFreshnessMaxAgeMs: 24 * 60 * 60 * 1000,
  componentRootsMax: 64,
  designComponentsMax: 2000,
  designPropertiesPerComponentMax: 40,
  designVariantsPerComponentMax: 500,
  designVariantsTotalMax: 20000,
  componentDependenciesPerEntityMax: 64,
  componentTokenRefsPerEntityMax: 128,
  componentVisualArtifactsMax: 64,
  componentInventoryVisualBytesMax: 16 * 1024 * 1024,
  adaptersMax: 8,
  rootsPerAdapterMax: 16,
  patternsPerAdapterMax: 32,
  projectFilesMax: 4000,
  projectFileBytesMax: 2 * 1024 * 1024,
  projectInputBytesMax: 128 * 1024 * 1024,
  projectTokensMax: 20000,
  projectComponentsMax: 4000,
  mappingsMax: 20000,
  componentMappingsMax: 2000,
  projectTargetsPerMappingMax: 8,
  contextsPerTokenMax: 16,
  findingsPerTokenRowMax: 16,
  findingsPerComponentRowMax: 64,
  suggestionsPerEntityMax: 5,
  taskProposalsMax: 200,
  reportBlockersMax: 32,
  apiPageDefault: 100,
  apiPageMax: 500,
  apiResponseBytesMax: 1024 * 1024,
  runnerTimeoutMs: 120000,
  runnerOutputBytesMax: 1024 * 1024
});
