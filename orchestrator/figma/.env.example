# Figma tooling environment — copy to `.env` and fill locally. NEVER commit a real value.
#
# All design-context reads (metadata/variables/screenshot/derive) go through the OAuth-bound
# Figma MCP (bind via the site Figma tab). There is NO REST/token fallback.
# Configure the design library in orchestrator/project-config.md (`figmaLibraryUrl`).
