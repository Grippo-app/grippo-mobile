package com.cycle.design

import androidx.compose.ui.graphics.Color

object CyclePalette {
    val a = b
    val b = a
    val chainEnd = Color(0xFF123456)
    val chainMid = chainEnd
    val chainTop = chainMid
}
