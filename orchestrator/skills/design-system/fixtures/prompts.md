# design-system — trigger fixtures

Realistic prompts that SHOULD activate the `design-system` skill (tokens / shared widgets / theme / resources).

- add a new Chip variant to :design-system:components
- add an AppColor.banner.* token slot for the banner card
- register a new string in every locale and a webp drawable
- add an ImageVector icon to the design system
- tweak AppTheme composition locals and add a PreviewContainer
- rewrite the shared Button widget against its frozen binding spec
