# platform-build-toolkit — trigger fixtures

Realistic prompts that SHOULD activate the `platform-build-toolkit` skill (build system / toolkit / shells).

- add a :toolkit:connectivity utility module
- add a version catalog entry and a build-logic convention plugin
- author the build.gradle.kts for the new module and touch settings.gradle.kts
- add a :compose-libs widget shared across products
- wire a deeplink intent filter in the thin :androidApp shell
- set up the iOS SwiftPackage framework and the :iosApp Xcode project
