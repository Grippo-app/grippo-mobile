# Backend contract tooling has no environment-file configuration.
#
# Integrations -> Backend owns the strict `environments.json` source manifest.
# Credentials are stored only through the Site in guarded, gitignored files
# under `.secrets/`; never put secrets in this file or project-config.md.
