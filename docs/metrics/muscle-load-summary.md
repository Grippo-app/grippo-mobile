# Muscle Load Summary — кто получает нагрузку

## Зачем это нужно
- Быстро понять, какие мышцы и группы получают больше всего работы.
- Увидеть перекос: например, если постоянно доминируют руки/плечи, а спина «не добирает».
- Отдельно смотреть:
    - **Stimulus** — «нагрузка от работы» (повторы + небольшой бонус за вес + поправки).
    - **Volume** — «тоннаж» (вес × повторы).

## Источники данных
- Тренировка → упражнения → подходы (iterations).
- У упражнения есть **bundles**: (мышца, процент). Это «куда уходит работа» в упражнении.
- У каждой мышцы есть параметры:
    - **size** — условная «ёмкость» мышцы (чем больше, тем труднее её “перегрузить” тем же объёмом).
    - **sensitivity** — условная «чувствительность» (чем больше, тем быстрее она ощущается нагруженной).

> Важно: bundles — это база. Но в расчёте мы дополнительно **подрезаем совсем маленькие доли**, чтобы «мелочь» не копилась неделями и не начинала доминировать.

---

## Как рассчитываем (по шагам)

### 1) Собираем упражнения
- Если пришёл список тренировок — берём все упражнения из всех тренировок.
- Если пришла одна тренировка — берём её упражнения.

✅ Ок  
⚠️ Не ок: если упражнений нет — результат будет пустым (это нормально).

---

### 2) Подгружаем данные упражнений (ExerciseExample)
- Для каждого упражнения берём `exerciseExample.id`.
- Подгружаем примеры, чтобы получить bundles (мышца + процент) и мета-данные (category, weightType, forceType).

✅ Ок  
⚠️ Не ок: если пример не подгрузился — такое упражнение просто не попадёт в расчёт.

---

### 3) Подгружаем группы мышц (MuscleGroup)
Нужно, чтобы:
- собрать отчёт **по группам**
- и привязать каждую мышцу к её группе.

✅ Ок

---

### 4) Считаем базовую нагрузку упражнения (rawLoad)

#### 4.1 Stimulus
Для каждого подхода:
- `reps` берём ≥ 0
- `weight` берём из `iteration.volume` (важно: у тебя это **вес**, не тоннаж)
- считаем небольшой бонус за вес:

`intensity = 1 + ln(1 + weight) / 8`

вклад подхода:

`reps * intensity`

Сумма по подходам = `rawLoad`.

✅ Ок: большой вес повышает stimulus, но логарифм сглаживает «не улетает в космос».  
🔧 Что подкрутить:
- если вес влияет **слишком сильно** → увеличь делитель (сейчас `/ 8`)
- если вес влияет **слишком слабо** → уменьшай делитель

#### 4.2 Volume
Для каждого подхода:
- `weight = iteration.volume`
- вклад = `weight * reps`
  Сумма по подходам = `rawLoad`.

✅ Ок: классический тоннаж.  
⚠️ Не ок: если когда-нибудь `iteration.volume` станет «тоннажом», то будет двойное умножение (но ты сказал — это вес).

---

### 5) Бонус «сложности упражнения» (только для Stimulus)
Идея: если упражнение реально задействует несколько мышц, оно чуть «весит» больше.

Как:
- берём проценты bundles
- считаем **effective muscle count (ENM)**: чем равномернее распределение — тем больше ENM
- считаем множитель:

`complexityFactor = 1 + k * ln(ENM)` (ограничен сверху)

✅ Ок: базовые движения становятся «тяжелее» по stimulus, чем изоляция при похожих цифрах.  
🔧 Что подкрутить:
- `COMPLEXITY_K` — сила бонуса (меньше → мягче, больше → сильнее)
- `COMPLEXITY_MAX_FACTOR` — верхняя крышка (чтобы не разгонялось)

---

### 6) Example factor (только для Stimulus)
Это грубая поправка «тип упражнения в среднем тяжелее/легче»:

- `weightTypeFactor`: free/fixed/bodyweight
- `categoryFactor`: compound/isolation

**Но важно:** для `compound` бонус делается **умнее**: он уменьшается, если упражнение и так уже получило бонус от сложности.  
То есть: чтобы не было «двойного буста» (сложность + compound).

✅ Ок: меньше перекрашивания всех compound одинаково.  
🔧 Что подкрутить:
- `EXAMPLE_COMPOUND_FACTOR` — общий бонус compound
- если кажется, что compound слишком «перетягивают» → уменьши это число

---

### 7) Делим нагрузку упражнения по мышцам (bundles)

#### 7.1 Нормализация процентов
- берём проценты bundles
- считаем `totalShare` (сумма)
- переводим в доли (0..1)

✅ Ок

#### 7.2 Главный фикс: подавление мелких долей (damping)
Чтобы 1–4% «стабилизации» не копились и не побеждали за неделю:

1) `ratio = share / totalShare`
2) `weight = ratio ^ SHARE_DAMPING_ALPHA`
3) нормализация: `finalRatio = weight / sum(weights)`
4) вклад мышцы = `load * finalRatio`

✅ Ок: мелочь становится ещё меньше, но сумма остаётся 100%.  
🔧 Что подкрутить:
- `SHARE_DAMPING_ALPHA`
    - выше → сильнее приоритет крупных долей (мелочь ещё меньше)
    - ниже → ближе к «как в базе»

---

### 8) ForceType правило (и для Stimulus, и для Volume)
Цель: в **push** упражнениях часто есть крошечная «стабилизация» (1–4%). Её мы дополнительно режем.

Правило:
- если `forceType == push`
- и **оригинальная доля** мышцы ≤ `4%`
- тогда умножаем вклад на `0.35`

✅ Ок: «мелочь» в push перестаёт копиться неделями.  
🔧 Что подкрутить:
- `PUSH_TINY_SHARE_MAX_PERCENT` (сейчас 4%)
    - выше → больше мышц попадут под резку
    - ниже → резка будет только у совсем крошечных долей
- `PUSH_TINY_SHARE_FACTOR` (сейчас 0.35)
    - меньше → режет сильнее
    - больше → режет слабее

---

### 9) Fatigue factor (только для Stimulus, мягко)
Сначала считаем “сырой коэффициент” мышцы:

`capacity = 0.80 + 0.40 * size`  
`sens = 0.80 + 0.40 * sensitivity`  
`fatigue = sens / capacity`

Но! Чтобы метрика отражала **«что ты тренировал»**, влияние fatigue делается **мягким**:

`multiplier = 1 + (fatigue - 1) * FATIGUE_IMPACT`

То есть:
- если fatigue = 1.20 и impact = 0.35 → множитель будет ~1.07 (а не 1.20)

✅ Ок: небольшая физиологическая поправка, но без «переворота смысла».  
🔧 Что подкрутить:
- `FATIGUE_IMPACT`
    - меньше → ближе к «что тренировал» (почти не влияет)
    - больше → ближе к «что устало» (влияние сильнее)

---

### 10) Суммируем вклад всех упражнений
В итоге получаем две карты:
- `stimulusLoad: MuscleEnum -> Float`
- `volumeLoad: MuscleEnum -> Float`

✅ Ок

---

## Как строим отчёты (важно: тут обновлённая логика)

### perMuscle (Stimulus / Volume)
- Берём сумму по всем мышцам.
- Каждой мышце даём **процент от общей суммы**.
- Сумма процентов по perMuscle ≈ 100%.

✅ Ок: проще воспринимать — «сколько процентов тренировки ушло в эту мышцу».  
⚠️ Не ок: если мышц очень много, хвост будет длинным (это норм).

### perGroup (Stimulus / Volume)
- Суммируем по группе (все мышцы группы).
- Показываем процент группы от общего.
- Сумма процентов по perGroup ≈ 100%.

✅ Ок

### dominance (только Stimulus)
- `top1SharePercent`: сколько забрала самая нагруженная мышца от общего.
- `top2SharePercent`: сколько забрали топ-2 вместе.

✅ Ок: быстрый индикатор «насколько всё упёрлось в одну мышцу».

---

## Как читать результат
- `perMuscle`: **проценты от общей нагрузки** (Stimulus).
- `perGroup`: **проценты от общей нагрузки** по группам (Stimulus).
- `volumePerMuscle`: то же самое, но по тоннажу.
- `volumePerGroup`: то же самое, но по тоннажу.
- `dominance`:
    - высокий `top1` → упражнение/тренировка очень «в одну мышцу»
    - высокий `top2` → всё упирается в две мышцы

---

## Быстрые подсказки «что подкрутить и когда»

### Проблема: «мелочь всё равно копится за неделю»
Подкручивай:
- `SHARE_DAMPING_ALPHA` ↑ (например 1.7–1.8)
- или `PUSH_TINY_SHARE_FACTOR` ↓ (например 0.25)

### Проблема: «слишком режет второстепенные, картинка кажется неверной»
Подкручивай:
- `SHARE_DAMPING_ALPHA` ↓ (например 1.4–1.5)
- или `PUSH_TINY_SHARE_FACTOR` ↑ (например 0.50)
- или `PUSH_TINY_SHARE_MAX_PERCENT` ↓ (например 3%)

### Проблема: «Stimulus слишком зависит от веса»
Подкручивай:
- `STIMULUS_LN_WEIGHT_SCALE` ↑ (например 10–12)

### Проблема: «Compound слишком доминируют всегда»
Подкручивай:
- `EXAMPLE_COMPOUND_FACTOR` ↓
- `COMPLEXITY_K` ↓
- и/или `COMPLEXITY_MAX_FACTOR` ↓

### Проблема: «усталость (fatigue) всё ещё меняет смысл»
Подкручивай:
- `FATIGUE_IMPACT` ↓ (например 0.20)

---

## Примеры сценариев (коротко)

### Новичок без весов
- `weight = 0` → intensity = 1 → stimulus всё равно работает по повторениям.
- volume будет маленький/нулевой, это нормально.

### Силовой стиль
- volume показывает рост тоннажа.
- stimulus показывает «нагрузку с учётом веса и базовых поправок».

### Часто “вылезает мелочь” (руки/стабилизация)
- damping + push-tiny rule как раз сделаны для этого:
  маленькие доли не должны побеждать неделю.
