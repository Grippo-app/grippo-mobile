// Root build script of the `build-logic` composite build. Intentionally empty:
// all convention-plugin configuration lives in build-logic/convention/build.gradle.kts,
// and the included build itself is defined by build-logic/settings.gradle.kts.
